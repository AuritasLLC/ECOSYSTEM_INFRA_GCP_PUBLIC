{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecosystem.resourceName" (list . "api-sap") }}
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
    cloud.google.com/backend-config: {{ printf "{\"default\":\"%s\"}" (include "ecosystem.resourceName" (list . "api-sap-backend")) | quote }}
  labels:
    app.kubernetes.io/name: api-sap
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: sap-api
spec:
  type: {{ .Values.service.type | default "ClusterIP" }}
  selector:
    app.kubernetes.io/name: api-sap
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.port | default 3020 }}
      targetPort: http
{{- end }}
